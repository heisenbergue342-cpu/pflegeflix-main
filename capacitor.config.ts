import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pflegeflixmain',
  appName: 'pflegeflix-main',
  webDir: 'dist'
};

export default config;
